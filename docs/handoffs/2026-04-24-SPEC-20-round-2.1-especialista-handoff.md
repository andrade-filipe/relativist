# Handoff — SPEC-20 Wave 1, Round 3 closure (especialista-specs)

**Status:** READY TO DISPATCH (paused for next session)
**Saved:** 2026-04-24 (validated 2026-04-24)
**Active bundle:** v2 Pre-DEV Spec Pipeline (Waves 1–5, no code) — `docs/next-steps.md`
**Master plan (reference):** `C:\Users\Filipe\.claude\plans\kind-shimmying-harbor.md`

> **Naming note (validation finding):** SPEC-20 §11 Change Log already has a "Round 3 — 2026-04-24" entry from commit d891d63 (the partial Round 3 that closed NF-002 via the ARG-005→ARG-006 rename). The work in this handoff is therefore labeled **Round 3 continuation** (closes the remaining 10 NFs from Round 2's CONDITIONAL_PASS), NOT a new "Round 2.1". The closure log filename and §11 entry must use Round 3 nomenclature.

---

## Where we stopped

`sdd-pipeline` ran and confirmed:
- Wave 1 = SPEC-20 (Elastic Grid), status NOT STARTED.
- Coherence brief: SKIP (3 briefings dated 2026-04-15 already cited in spec).
- spec-critic Round 1: SKIP (already done — `docs/spec-reviews/archive/SPEC-REVIEW-20-round-1-2026-04-24.md`).
- spec-critic Round 2: DONE — verdict **CONDITIONAL_PASS** (`docs/spec-reviews/archive/SPEC-REVIEW-20-round-2-2026-04-24.md`).
- 30/30 Round 1 findings closed; 11 fresh NFs opened in Round 2 (2 HIGH, 6 MED, 3 LOW).
- NF-002 (ARG-005 name collision) already discharged in commit `d891d63` (rename ARG-005→ARG-006 in TCC root, ARG-006 written, theory-bridge updated).

**Next action:** dispatch `especialista-specs` for **Round 2.1 closure** (10 remaining NFs + 2 surgical polish items + 1 optional sketch).

**IMPORTANT — Memory dispatch policy (`feedback_especialista_specs_dispatch.md`):**
For Relativist spec edits, the user runs `especialista-specs` from the **TCC root session**, NOT from inside the relativist subdir. Use the prompt block in §3 of this file as the agent prompt verbatim.

---

## 1. Target file

`codigo/relativist/specs/SPEC-20-elastic-grid.md` (currently `Status: Draft — Round 2`, 1144 lines).

## 2. Closure log to produce

`codigo/relativist/docs/spec-reviews/SPEC-REVIEW-20-round-3-2026-MM-DD.md` (replace `MM-DD` with run date). This is a continuation of the partial Round 3 already recorded in §11 (commit d891d63).

End-state of SPEC-20 status field (currently STALE — line 3 reads `Draft — Round 2 (post-SPEC-REVIEW-20-round-1-2026-04-24)`; update it):

- If the specialist closes all 10 remaining NFs inline with no structural escalation: bump to `Reviewed v2` (interpretation: Round 2 adversarial review reached CONDITIONAL_PASS; Round 3 closure landed; spec is review-cleared for Stages 1+2).
- If any NF is judged structurally non-trivial enough to warrant a fresh spec-critic adversarial pass: bump to `Draft — Round 3 (closure landed; pending spec-critic Round 3 review)`. This is the ONLY case requiring a relativist-side spec-critic round; Round 2 verdict explicitly states Round 3 is **AVOIDABLE**, so prefer the first option.

## 3. Agent prompt (paste verbatim into especialista-specs)

```
Mode: Round 3 closure for SPEC-20 (Elastic Grid) — continuation of the partial
Round 3 already recorded in §11 (commit d891d63 closed NF-002 only). This pass
closes the remaining 10 NFs from Round 2's CONDITIONAL_PASS verdict
(spec-critic Round 2, 2026-04-24).

INPUTS (read in this order):
1. codigo/relativist/specs/SPEC-20-elastic-grid.md — target spec (1179 lines as of 2026-04-24, post-d891d63).
2. codigo/relativist/docs/spec-reviews/archive/SPEC-REVIEW-20-round-2-2026-04-24.md
   — adversarial review with 11 NF findings + TODO list (§"Specialist-em-specs TODO list").
3. codigo/relativist/docs/theory-bridge.md — canonical ARG/DISC/REF index.
4. ARG-005 (delta border completeness) at the path listed in theory-bridge.md.
5. ARG-006 (mixed-trace recoverability) at the path listed in theory-bridge.md.

CONTEXT — what is already done:
- NF-002 (ARG-005 name collision) is CLOSED via commit d891d63. SPEC-20
  frontmatter (line 7), §3.3 R29a area (lines 268-271), §3.7 R39 gate table
  (lines 485-489), §5.1 (line 876) and §11 already reference ARG-006
  (mixed-trace recoverability) where appropriate. ARG-005 is now reserved
  exclusively for SPEC-19 §8 OQ-1 (delta border completeness). Theory-bridge.md
  reflects this rename. DO NOT redo the ARG-005→ARG-006 substitution.

KNOWN STALE/INCONSISTENT STATE TO REPAIR DURING THIS PASS:
1. Status field (specs/SPEC-20-elastic-grid.md line 3) is STALE — still reads
   "Draft — Round 2 (post-SPEC-REVIEW-20-round-1-2026-04-24)". Update it per
   §"End-state of SPEC-20 status field" above.
2. §11 Change Log line 1101 has a misattribution: it says the remaining
   Round-2 NFs "are addressed by SPEC-CRITIC Round 3 in Wave 1 of the v2
   Pre-DEV bundle (separate work)". The Round 2 review verdict explicitly
   states "Round 3 is AVOIDABLE" and the owner of NF closure is the
   ESPECIALISTA, not spec-critic. Rewrite that sentence to read approximately:
   "the other Round-2 NFs are addressed by ESPECIALISTA EM SPECS in this
   Round 3 closure pass". Spec-critic only re-engages if a closure is judged
   structurally non-trivial (escalation path).

WORK TO DO (10 remaining NFs + 2 surgical polish items):

  HIGH:
    NF-001  SPEC-02 Net::union dependency — preferred fix: add §3.8 amendment
            A7 (`Net::union(self, other: Net) -> Net` under disjoint-AgentId
            precondition). Alternative: rewrite §4.2.2 v1-step-4 and delta-step-4
            to use a synthetic single-partition PartitionPlan + merge().
            Pick A7 unless rewrite is materially cleaner.
    NF-003  R4-delta self-worker symmetry — add explicit clause that the
            in-process self-worker MUST run SPEC-19 R24's full worker-side delta
            state machine via ChannelTransport; add test EG-U4-delta-wire-symmetry
            asserting RoundResult.border_deltas shape-equivalence with a remote
            worker on the same partition.

  MEDIUM:
    NF-004  Enumerate SPEC-19 R45 metric field names inline in R38a (commit the
            non-collision audit to the written record).
    NF-005  Promote Open Issue #4 to §3.8 as A8 (or A7 if NF-001 took A8):
            SPEC-19 R38 reconstruct() signature extension to accept optional
            third argument `reclaimed_partitions: Vec<Partition>`.
    NF-006  Expand §3.8 A3/A4 with error-return signatures:
              A3 -> Result<Range<u32>, PartitionError::BorderIdSpaceExhausted>
              A4 -> Result<Partition, PartitionError::NewRangeTooSmall>
    NF-007  Add R26a for D == K_eff edge case — hybrid-fallback path discards
            retained_last_acked, non-hybrid path transitions to Error;
            FinalStateRequest skipped; reconstruct from retained_initial only.
    NF-011  Fix R23a retention policy wording (reclaim consumes the slot) AND
            correct R31 memory bound from O(|W_ever_active|) to
            O(|W_currently_active + W_pending_reclaim|).

  LOW:
    NF-008  Upgrade TimerKind derivation from "e.g. as kind as u32" to MUST,
            with `#[repr(u32)]` on TimerKind. One-line edit.
    NF-009  Align JoinNackReason::ProtocolVersionMismatch shape with SPEC-19
            R37 RegisterNack — either reuse the same struct or explicitly
            acknowledge divergence.
    NF-010  Split EG-U15 into EG-U15a (Register path) and EG-U15b (JoinRequest
            path).

  POLISH (NOT NF, but in TODO list):
    Item 12 §3.8 A3 amendment number mismatch — A3 currently targets "SPEC-04
            R15" but border_id allocation is R16-R18; retarget A3 properly.
    Item 13 §5.1 proof sketch polish — OPTIONAL: pull "reduce to Passo 11 via
            clean-boundary re-split" argument out of R39-G1-elastic-departure
            into §5.1 as a proper sketch. Skip if cost > value.

DELIVERABLES:
1. Edit codigo/relativist/specs/SPEC-20-elastic-grid.md applying every fix above
   PLUS the two stale-state repairs (status field + §11 line 1101 wording).
2. Append a new sub-section to §11 Change Log:
   "### Round 3 (continuation) — YYYY-MM-DD — NF closure pass"
   enumerating each NF closed (NF-001, NF-003..011) with diff summary. Place it
   after the existing "### Round 3 — 2026-04-24 (housekeeping after TCC-root
   theoretical work)" entry that closed NF-002.
3. Bump status field from "Draft — Round 2 (post-SPEC-REVIEW-20-round-1-...)"
   to "Reviewed v2" (preferred) or "Draft — Round 3 (closure landed; pending
   spec-critic Round 3 review)" (only if structural escalation is needed). The
   Round 2 verdict explicitly says Round 3 is AVOIDABLE — prefer "Reviewed v2".
4. Write closure log at
   codigo/relativist/docs/spec-reviews/SPEC-REVIEW-20-round-3-YYYY-MM-DD.md
   following the SPEC-REVIEW-19-section-3.4-D-005-2026-04-23-REREVIEW-R3.md
   structure (per-NF: verdict, evidence, diff pointer). Title it as a
   continuation of the partial Round 3 from d891d63 (which only closed NF-002).

CONSTRAINTS:
- Documentation only. NO src/ or tests/ edits. Test counts must remain
  1181 default / 1224 zero-copy.
- ARG/DISC/REF identifiers must match theory-bridge.md exactly. Any cited ID
  not in the bridge is a hard block — surface it instead of inventing.
- Stay within "specs/" and "docs/spec-reviews/" — do NOT touch src/, tests/,
  docs/backlog/, or docs/tests/. Stage 1 (task-splitter) and Stage 2
  (test-generator) come AFTER your closure log lands.

When done, report: (a) closure log path, (b) status field new value, (c)
NF-by-NF one-line summary, (d) any item left open or escalated to Round 3.
```

## 4. After especialista-specs returns

1. Verify the closure log exists and SPEC-20 §11 Change Log has the Round 2.1 row.
2. Decide whether Round 3 (spec-critic re-review) is needed. Round 2 verdict says **AVOIDABLE**; only invoke spec-critic Round 3 if specialist flagged any NF as needing re-adversarial review or if status was bumped to `Draft — Round 3`.
3. If status reaches `Reviewed v2`, advance pipeline to Wave 1 / Stage 1: dispatch `task-splitter` on SPEC-20.
4. Then Stage 2: dispatch `test-generator` on the SPEC-20 backlog tasks.
5. Tracking + commit (Conventional Commits per `git-commits` skill, scope `SPEC-20`).
6. Update `docs/next-steps.md` Wave 1 status row.

## 5. Open follow-up coordination (out of Wave 1 scope)

- ARG-005 (delta border completeness) — DEBATEDOR work in TCC root, gates SPEC-19 R38/R39/R40 final D-008 sign-off. Already written but tracked as "CLOSED" in theory-bridge per commit d891d63. If specialist needs to revisit, escalate.
- ARG-006 (mixed-trace recoverability) — same status; gates SPEC-20 R29a/R39 final sign-off. Already written and CLOSED per d891d63.

## 6. Files NOT to touch in this round

- `src/`, `tests/` (no DEV until Wave 1 closes Stages 1+2)
- `docs/backlog/`, `docs/tests/` (Stages 1 and 2, after this round)
- `OBJETIVO_TCC.md` (read-only, ever)
- TCC root `discussoes/`, `biblioteca/`, `artigo/` (other agents' territory)

## 7. Quick-resume checklist (next session)

- [ ] `cd C:\Users\Filipe\Desktop\TCC_interaction_combinators_for_grid_computing` (TCC root, NOT relativist subdir)
- [ ] Open this handoff: `codigo/relativist/docs/handoffs/2026-04-24-SPEC-20-round-2.1-especialista-handoff.md`
- [ ] Dispatch `especialista-specs` with the prompt in §3 above
- [ ] After agent returns, follow §4 steps
- [ ] Stage 1 (task-splitter) is the next pipeline action after closure log lands

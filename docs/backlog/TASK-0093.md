# TASK-0093: Implement run_worker (worker loop)

**Spec:** SPEC-06
**Requirements:** R18, R23, R27, R32
**Priority:** P0 (critical path)
**Status:** TODO
**Depends on:** TASK-0085, TASK-0086, TASK-0087, TASK-0082
**Blocked by:** none
**Estimated complexity:** M (50-200 LoC)

## Context

`run_worker` is the top-level function for a worker process. It connects to the coordinator (via `connect_with_retry`), then enters a loop: receive `AssignPartition` -> reduce locally -> rebuild `free_port_index` -> send `PartitionResult`. The loop exits when a `Shutdown` message is received. This implements the worker FSM from R27: Connecting -> Idle -> Reducing -> Sending -> Idle -> ... -> Done.

## Acceptance Criteria

- [ ] `run_worker` is implemented in `src/protocol/worker.rs`
- [ ] Function is async, takes `&NodeConfig` as parameter
- [ ] Connects to coordinator via `connect_with_retry` (TASK-0087)
- [ ] Enters a loop that calls `recv_frame` to get the next message
- [ ] On `Message::AssignPartition`: reduces the partition's subnet via `reduce_all`, rebuilds `free_port_index`, computes `WorkerRoundStats`, sends `PartitionResult` via `send_frame`
- [ ] On `Message::Shutdown`: logs and breaks from the loop
- [ ] On unexpected message: returns `ProtocolError::UnexpectedMessage`
- [ ] Returns `Ok(())` on clean shutdown

## Files to Create/Modify

- `src/protocol/worker.rs` -- **modify**: add `run_worker` function

## Key Types / Signatures

```rust
/// Runs the worker loop: connect, receive partitions, reduce, send results.
///
/// Implements the worker FSM (R27):
/// Connecting -> Idle -> Reducing -> Sending -> Idle -> ... -> Done.
pub async fn run_worker(config: &NodeConfig) -> Result<(), ProtocolError>
```

## Test Expectations

- Integration test: coordinator sends one AssignPartition + Shutdown, verify worker returns Ok(()) and sends correct PartitionResult
- Integration test: coordinator sends multiple rounds (AssignPartition, AssignPartition, Shutdown), verify worker processes all rounds
- Integration test: coordinator sends unexpected message, verify worker returns UnexpectedMessage
- Unit test: verify WorkerRoundStats contains correct agents_before, agents_after, local_redexes

## Dependencies Context

- `connect_with_retry` from TASK-0087 (`src/protocol/worker.rs`)
- `send_frame` from TASK-0085 (`src/protocol/frame.rs`)
- `recv_frame` from TASK-0086 (`src/protocol/frame.rs`)
- `Message` enum from TASK-0082 (`src/protocol/types.rs`)
- `NodeConfig` from TASK-0084 (`src/protocol/config.rs`)
- `reduce_all()` from SPEC-03 reduction module
- `rebuild_free_port_index()` from SPEC-05 merge module (Section 4.3)
- `WorkerRoundStats` from SPEC-05 merge module
- `count_live_agents()` utility from the net module

## Notes

- The worker does NOT know the total number of rounds. It simply loops until receiving Shutdown.
- `rebuild_free_port_index` scans the subnet for FreePort references in the port array and rebuilds the `HashMap<u32, PortRef>`. This is necessary because reduction may change which ports are connected to FreePort sentinels (SPEC-05, Section 4.3).
- The worker logs each round with `tracing::info!` including round number, worker_id, agents_before, agents_after, local_redexes.
- Unlike the coordinator, the worker has no timeouts for receiving messages. It simply waits indefinitely for the next AssignPartition or Shutdown.

# TASK-0300: Add transport dependencies to Cargo.toml

**Spec:** SPEC-17 (Transport Abstraction and Tuning)
**Phase:** v2 — Milestone M1 (Transport Optimization)
**Priority:** P0
**Complexity:** S (~5 LoC)
**Depends on:** none
**Blocks:** TASK-0301, TASK-0303, TASK-0304

## Description

Add `socket2` and `async-trait` crates to `Cargo.toml` as always-on dependencies (SPEC-17 R21, R4).

- `socket2`: Required for TCP keepalive configuration (`SockRef`, `TcpKeepalive`). Always-on per R21.
- `async-trait`: Required for object-safe `Transport` trait with async methods (R4). Native async traits (Rust 1.75+) do NOT support `Box<dyn Transport>` dispatch.

## Acceptance Criteria

1. `socket2` is listed under `[dependencies]` with appropriate version
2. `async-trait` is listed under `[dependencies]` with appropriate version
3. `cargo build` succeeds
4. All 690 tests still pass

## Files to Modify

- `Cargo.toml`: add 2 dependency lines

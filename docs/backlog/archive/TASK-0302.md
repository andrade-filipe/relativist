# TASK-0302: Add transport field to NodeConfig

**Spec:** SPEC-17, Section 3.6 (R25), Section 4.6
**Phase:** v2 — Milestone M1
**Priority:** P0
**Complexity:** S (~20 LoC)
**Depends on:** TASK-0301
**Blocks:** TASK-0308, TASK-0309, TASK-0310

## Description

Add `transport: TransportConfig` field to the existing `NodeConfig` struct in `src/protocol/config.rs` (R25). Update the `Default` impl to set `transport: TransportConfig::default()`.

## Acceptance Criteria

1. `NodeConfig` has a `pub transport: TransportConfig` field
2. `NodeConfig::default().transport` equals `TransportConfig::default()` (test UT3)
3. Existing `NodeConfig` tests still pass (existing T1-T4 may need update for new field)
4. All 690 tests still pass — all code constructing `NodeConfig { .. }` must be updated with `..NodeConfig::default()` or explicit `transport` field

## Impact Assessment

Any code constructing `NodeConfig` without `..Default` will fail to compile. Places to update:
- `src/commands.rs`: `run_coordinator_command`, `run_worker_command`, `run_local_command`
- `src/protocol/coordinator.rs`: test helpers
- `src/protocol/worker.rs`: test helpers
- All tests that construct `NodeConfig` directly

## Files to Modify

- `src/protocol/config.rs`: add field, update Default, update tests
- `src/commands.rs`: update NodeConfig construction
- Tests in coordinator.rs, worker.rs: ensure `..NodeConfig::default()` covers the new field

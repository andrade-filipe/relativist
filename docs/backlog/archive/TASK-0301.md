# TASK-0301: Define TransportBackend enum and TransportConfig struct

**Spec:** SPEC-17, Section 3.6-3.7 (R23-R27)
**Phase:** v2 — Milestone M1
**Priority:** P0
**Complexity:** S (~60 LoC)
**Depends on:** TASK-0300
**Blocks:** TASK-0302, TASK-0304, TASK-0310

## Description

Define `TransportBackend` enum and `TransportConfig` struct in `src/protocol/config.rs`.

### TransportBackend (R27)

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum TransportBackend {
    Tcp,
    Unix,
    Channel,
}
```

### TransportConfig (R23-R24, R26)

All fields as specified in SPEC-17 R24:
- `backend: TransportBackend` (default: `Tcp`)
- `tcp_nodelay: bool` (default: `true`)
- `send_buffer_bytes: Option<usize>` (default: `Some(4_194_304)`)
- `recv_buffer_bytes: Option<usize>` (default: `Some(4_194_304)`)
- `keepalive_idle: Option<Duration>` (default: `Some(30s)`)
- `keepalive_interval: Duration` (default: `10s`)
- `keepalive_count: u32` (default: `3`)
- `unix_socket_path: Option<PathBuf>` (default: `None`)

Must derive `Debug, Clone` and implement `Default` with spec values.

## Acceptance Criteria

1. `TransportBackend` has exactly 3 variants with all required derives
2. `TransportConfig` has all 8 fields with correct types
3. `TransportConfig::default()` returns spec-mandated values (R24)
4. Tests: UT1 (defaults), UT2 (variants + derives)
5. All 690 tests still pass

## Files to Modify

- `src/protocol/config.rs`: add types + tests

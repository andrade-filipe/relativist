# TASK-0061: Define WorkerRoundStats struct

**Spec:** SPEC-05
**Requirements:** R37
**Priority:** P0 (critical path)
**Status:** TODO
**Depends on:** none
**Blocked by:** none
**Estimated complexity:** S (< 50 LoC)

## Context

SPEC-05 R37 (upgraded to MUST in Revised v3, SC-001) specifies a `WorkerRoundStats` structure that captures per-worker statistics for a single round. The struct now includes 6 fields: the original 4 fields plus `reduce_duration_secs` and `interactions_by_rule` (added by SC-001 to resolve SPEC-11 OQ-1). The struct MUST derive `serde::Serialize` and `serde::Deserialize` for wire transmission (SPEC-06 R12). This is the canonical definition; SPEC-11 OQ-1 is resolved.

## Acceptance Criteria

- [ ] `WorkerRoundStats` struct is defined in `src/merge.rs`
- [ ] `WorkerRoundStats` derives `Debug, Clone, serde::Serialize, serde::Deserialize`
- [ ] Contains fields: `worker_id`, `agents_before`, `agents_after`, `local_redexes`
- [ ] Contains field: `reduce_duration_secs: f64` (wall-clock duration of reduce_all)
- [ ] Contains field: `interactions_by_rule: [u64; 6]` (per-rule counts: [CON-CON, CON-DUP, CON-ERA, DUP-DUP, DUP-ERA, ERA-ERA])
- [ ] The module compiles with `cargo check`

## Files to Create/Modify

- `src/merge.rs` -- modify: add `WorkerRoundStats` struct definition

## Key Types / Signatures

```rust
/// Statistics of a single worker in a specific round.
/// Canonical definition: SPEC-05 R37. Resolves SPEC-11 OQ-1.
#[derive(Debug, Clone, serde::Serialize, serde::Deserialize)]
pub struct WorkerRoundStats {
    /// Identifier of the worker.
    pub worker_id: WorkerId,
    /// Number of live agents in the partition before local reduction.
    pub agents_before: usize,
    /// Number of live agents in the partition after local reduction.
    pub agents_after: usize,
    /// Number of local redexes reduced by this worker.
    pub local_redexes: usize,
    /// Wall-clock duration of reduce_all for this worker (seconds).
    pub reduce_duration_secs: f64,
    /// Per-rule interaction counts:
    /// [CON-CON, CON-DUP, CON-ERA, DUP-DUP, DUP-ERA, ERA-ERA].
    pub interactions_by_rule: [u64; 6],
}
```

## Test Expectations

- Unit test: create a `WorkerRoundStats` with all 6 fields, verify all fields are accessible
- Unit test: serialization round-trip (serialize to bincode, deserialize, verify fields)
- Unit test: `interactions_by_rule` has exactly 6 elements

## Dependencies Context

- `WorkerId` is `pub type WorkerId = u32;` (defined in the partition module, SPEC-04).
- If `WorkerId` is not yet exported, use `u32` directly and add a `// TODO: use WorkerId from partition` comment.

## Notes

- This is a simple data-only struct. No methods are required at this stage.
- **SPEC-05 v3 changes (SC-001):** R37 upgraded from SHOULD to MUST. Two fields added: `reduce_duration_secs` and `interactions_by_rule`. serde derives added. This definition is now canonical; SPEC-11 OQ-1 is resolved. TASK-0152 is now **OBSOLETED** by this task (the extension it planned is already included here).

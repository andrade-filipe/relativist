# TASK-0337: default_registry() with 5 codecs (R19)

**Phase:** 4 (SPEC-27)
**Size:** S (~80 LoC inc. tests)
**Depends on:** TASK-0336 (registry struct), TASK-0332 (ChurchArithmeticCodec), TASK-0335 (LambdaCodec)
**Spec:** SPEC-27 R19

---

## Deliverable

```rust
/// Returns a registry pre-populated with all built-in codecs (R19).
pub fn default_registry() -> EncoderRegistry {
    let mut r = EncoderRegistry::new();
    r.register(Box::new(ChurchArithmeticCodec::new(ChurchOp::Add))).unwrap();
    r.register(Box::new(ChurchArithmeticCodec::new(ChurchOp::Mul))).unwrap();
    r.register(Box::new(ChurchArithmeticCodec::new(ChurchOp::Exp))).unwrap();
    r.register(Box::new(ChurchArithmeticCodec::new(ChurchOp::SumOfSquares))).unwrap();
    r.register(Box::new(LambdaCodec::new())).unwrap();
    r
}
```

The 4 Church codecs each MUST report a unique `name()` (`church_add`, `church_mul`,
`church_exp`, `church_sum_of_squares`) — adjust `ChurchArithmeticCodec::name()` if it
currently returns a single shared name (verify before implementing).

## Acceptance

- `default_registry().list().len() == 5`
- Names match R19 exactly: `["church_add", "church_exp", "church_mul", "church_sum_of_squares", "lambda"]` (sorted).
- Each codec round-trips at least one well-formed input.
- `cargo test --workspace` count: 765+ → 770+ (≥ +5).
- Clippy + fmt clean.

## Out of Scope

- CLI `encoders list` command — TASK-0339.
- Per-encoder validation tests beyond a smoke round-trip.
